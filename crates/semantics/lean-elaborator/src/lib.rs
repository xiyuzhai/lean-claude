#![feature(let_chains)]

// Placeholder for elaborator implementation
pub struct Elaborator;

impl Elaborator {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Elaborator {
    fn default() -> Self {
        Self::new()
    }
}
